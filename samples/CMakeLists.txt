cmake_minimum_required (VERSION 3.18)

# Each subdirectory is a self-contained sample. The cert-generation
# helper script (samples/scripts/generate_certs.sh) is shared.
add_subdirectory(server_client)
add_subdirectory(streaming)
